# Gunakan PHP + Apache
FROM php:8.1-apache

# Salin semua file ke dalam container
COPY . /var/www/html/

# Aktifkan .htaccess / mod_rewrite jika diperlukan
RUN a2enmod rewrite

# Set working directory jadi folder public
WORKDIR /var/www/html/public

# Buka port 80
EXPOSE 80

#bisa mass